apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: A simple application to deploy on Kubernetes - named ${{values.app_name}}
  annotations:
    github.com/project-slug: xcirel/${{values.app_name}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental
